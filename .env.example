VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
# Activa el gate de auth (login Google + aprobación). "false" = app sin auth.
VITE_AUTH_ENABLED="false"
# Secrets de Supabase (NO van en el cliente). Ver supabase/TELEGRAM.md:
#   GEMINI_API_KEY
#   TELEGRAM_BOT_TOKEN
#   TELEGRAM_WEBHOOK_SECRET
#   TELEGRAM_BOT_USERNAME
# Keep-alive (GitHub Actions → repo secrets, no VITE_):
#   SUPABASE_URL          (= mismo valor que VITE_SUPABASE_URL)
#   SUPABASE_ANON_KEY     (= mismo valor que VITE_SUPABASE_ANON_KEY)
